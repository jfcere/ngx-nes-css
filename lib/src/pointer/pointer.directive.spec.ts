import { NesPointerDirective } from './pointer.directive';

describe('PointerDirective', () => {
  it('should create an instance', () => {
    const directive = new NesPointerDirective();
    expect(directive).toBeTruthy();
  });
});
